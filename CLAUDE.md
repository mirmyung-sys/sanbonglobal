# 산본글로벌어학원 홈페이지 — 작업 기준

`index.html` 한 파일이 사이트 전부입니다 (약 450KB, 페이지 40개).
빌드 과정 없이 GitHub Pages로 그대로 서비스됩니다.

## 손대기 전에 읽을 것

- 화면을 새로 만들 때 **`:root` 토큰 밖의 색·글자 크기를 쓰지 않습니다.** 토큰은 `index.html` 맨 위 `:root{}` 블록에 있습니다.
- `--red` 는 이름과 달리 **남색**이 들어 있는 옛 변수입니다. 새 코드에서는 `--brand`(적색) / `--navy`(남색)를 쓰세요.
- 문구는 학원 대표(명충건)가 실제로 지킬 수 있는 것만 씁니다. 지어낸 후기·못 지킬 할인율은 넣지 않습니다.

## 색

| 역할 | 토큰 | 값 |
|---|---|---|
| 브랜드 적색 — CTA·강조 | `--brand` | `#c81f2b` |
| 적색 그라데이션 짝 | `--brand-lt` | `#e0453e` |
| 기관 남색 — 헤더·히어로 패널 | `--navy` | `#1D2D4F` |
| 금색 — 엠블럼·격식 | `--gold` | `#C6A15B` |

과목·요일 색은 `--sub-*` 로 따로 있습니다 (강의관·달력 전용):
영어회화 초록 `#0e8a5a` / 월·수 파랑 `#2f6fb0` / 화·목 보라 `#7658c8` / 토요일 금색 `#C6A15B` / 월~금 스파르타 초록 `#0e8a5a`.

중성색은 순회색이 아니라 **남색 쪽으로 살짝 기울인** 램프입니다:
`--ink #1a1f2b` → `--ink2 #3f4757` → `--ink3 #5c6472` → `--muted #8a90a0` → `--line #e3e6ee` → `--line2 #eef1f6` → `--surf #f6f7fa`.

## 글자

11 / 12 / 13 / 14 / 15 / 16 / 18 / 20 / 22 / 26 / 30 / 36 px.
**반쪽 픽셀(12.5px 등)을 쓰지 않습니다.** 본문 15px, 설명 14px, 라벨 13px, 각주 12px이 기본입니다.
히어로 대형 문구만 `clamp()`로 예외를 둡니다.

여백은 4의 배수(`--s1`~`--s8`), 모서리는 `--r-sm/md/lg/pill`, 그림자는 `--sh-1/2/3`.

## 할인 규칙 (건드리면 매출에 직접 영향)

대표님 방침: **할인으로 승부보지 않습니다.** 할인 종류를 늘리지 말고, 혜택은 무료수강으로 주세요.

- 온라인 즉시할인 **상한은 30%** 입니다 (`DCAP=0.30`, 2026-09-04 대표님 지시).
- **계산은 「전부 더해서 상한으로 자른 뒤 정가에 한 번만」 뺍니다.** 예전에는 이벤트를 뺀 금액에 재등록을 또 곱해서(10%+5% → 실제 14.5%) 화면의 %와 실제 금액이 달랐습니다. **다시 곱셈으로 바꾸지 마세요.** `sumRate()` / `evCalc()` 가 그 규칙입니다.
- **`sgAudit()`** 가 페이지 로드 시 자동으로 돌면서 모든 조합의 표시%와 실제 차감액이 같은지 검사합니다. 할인 코드를 고치면 콘솔에서 이게 초록으로 뜨는지 확인하세요.
- 현재 즉시할인: **오픈이벤트 10%** (10/1~11/30), **새벽반 조조 30%** (상시·새벽반 자동).
- **재등록 얼리버드 5%**는 `offline:true` — 재원생인지 온라인에서 확인이 안 되므로 즉시 할인이 아니라 **명부 확인 후 환급**입니다.
- **환승 이벤트가 12월에 부활합니다** (2026-09-04 대표님 지시로 방침 변경). 12/1~12/31 · 환승 15% + 얼리버드 10% + 재등록 5% = **최대 30%**. **정규반·시험대비반만이고 1:1 퍼스널에는 붙지 않습니다** (`nopers:true`). 날짜가 되면 자동으로 켜집니다.
- **가족·친구 동반 이벤트도 삭제했습니다.** 2인 동시 등록을 온라인에서 확인할 방법이 없습니다. 다시 넣지 마세요.
- 무료 개월이 포함된 상품과 이벤트 할인은 **중복되지 않고 더 큰 쪽**이 적용됩니다 (`evCalc`).
- **1대1 무료수강은 할인이 아닙니다.** (2026-09-01 대표님 확정) **10시간권에는 무료수강이 없습니다.** 20시간 → 정규반 1개월(원어민 회화~시험대비 중 선택, 18~21만원 상당), 40시간 → 2개월. 화면에서 초록으로, 할인(빨강)과 구분해서 표시합니다.
- **1대1 시간권 단가** (2026-09-01 대표님 확정): 기준은 20시간권 — 영어 한인 5만/h, 영어 원어민·시험대비 6만/h, 일본어·중국어는 전부 5만/h. **10시간권은 시간당 5천원 높습니다** (55만/65만). 20시간으로 유도하는 구조라 균일가로 바꾸지 마세요.

- **새벽반 조조할인 30%** (2026-09-04 대표님 지시로 전 기간 확대): 영어·일본어·중국어 새벽반은 **2·3·4·6개월 전부 30%** 입니다 — 252,000 / 378,000 / 504,000 / 756,000원. 6개월 고정이던 예전 분기는 제거했습니다. 할인은 `evRate()` 안의 `dawnRate()` 가 자동으로 더하므로 **가격을 따로 계산하지 마세요.** 30%가 이미 상한이라 오픈이벤트와 중복되지 않고, 결제창에서 이벤트 선택이 잠깁니다. 상품권(충전금) 결제 불가(`_noGift`)는 그대로입니다.

정규 초중급반은 정가(list)를 두지 않아 640,000원에서 10%가 그대로 빠집니다(→576,000원). 스파르타 2개월처럼 정가가 있는 상품은 2개월 결제 할인이 10%보다 커서 이벤트 할인이 붙지 않습니다.

## 확인 방법

로컬에 서버가 필요 없습니다. Playwright로 `file://` 열어서 확인합니다.
날짜에 따라 달라지는 화면(오픈이벤트 10/1~11/30, 사전예약 배너 ~9/30)은 `addInitScript`로 `Date`를 바꿔 검증합니다.

## 상품권 보너스율 — 함부로 올리지 마세요 (2026-08-30)

충전금은 **정가로 차감**되므로, 보너스율이 곧 실질 할인율입니다.
보너스 100% → 실질 단가가 **정가의 50%**. 보너스 50% → **정가의 67%**.

현금 최고 혜택은 **정가의 60%**입니다 (회화 6개월 등록 108만원 ÷ 10개월 = 월 108,000원).
보너스가 67%를 넘으면 **상품권이 현금 장기등록보다 싸져서**, 108만원을 현금으로 낸 회원이
50만원을 충전한 회원보다 비싸게 다니게 됩니다. 그러면 아무도 장기등록을 하지 않고
전부 상품권으로 갑니다 — 상품권은 매출이 아니라 **부채**입니다.

그래서 보너스는 **50%가 상한**입니다. 현재 회화 기준 실질 월 단가:

| | 월 실질 |
|---|---|
| 2개월 단과 | 180,000원 |
| 3개월 등록 (총 4개월) | 135,000원 |
| **상품권 충전금** | **125,000원** |
| 4개월 등록 (총 6개월) | 120,000원 |
| 6개월 등록 (총 10개월) | 108,000원 |

현금 장기등록이 항상 가장 싸야 합니다. 이 순서가 깨지면 가격표가 무너집니다.
무한수강권(정가의 54%)은 충전금으로 결제할 수 없게 막아 두었습니다 (`nodisc` → `_noGift`).

## 화면에 데모 값을 남기지 마세요 (2026-08-30)

대표님이 두 번 잡아내셨습니다 — 적립금 「12,000원」과 AI 학습 달력의 「24일 경과 · 4강 결석」.
둘 다 개발 중 화면을 채우려고 넣은 값이었는데, 처음 오신 분에게는 **남의 계정을 보고 있는 것처럼** 보입니다.

- 잔액·포인트·출석·진도는 **0 또는 빈 상태**가 기본입니다.
- 예시를 보여줘야 하면 숫자 대신 **「예시」 라벨**을 붙이거나, 실제 개강일 기준 **예정표**만 보여줍니다.
- 커밋 전에 `데모`, `샘플`, `테스트` 주석이 화면에 나가는 값에 붙어 있지 않은지 확인합니다.

## 이 저장소는 절대 push 하지 않습니다 (2026-09-01)

로컬과 원격의 히스토리가 갈라져 있습니다 — 로컬이 97개 앞서고, 원격이 13개 앞섭니다.
그 원격 13개가 **대표님이 GitHub 웹에서 직접 올리신 커밋**입니다.

밀어넣으면 대표님 업로드 기록이 사라집니다. 게다가 이 세션에서는 프록시가 막고 있어
`git push` 자체가 되지 않습니다.

**배포는 이 경로로만 합니다:**

1. 파일을 고친다 → `node --check` + `smoke.py`로 확인
2. 푸터 버전 도장을 올린다 (`ftVer`)
3. 로컬 커밋 (기록 보관용)
4. **Claude가 브라우저로 직접 업로드** → 1~2분 뒤 라이브 반영
   (2026-09-03 대표님 지시: "앞으로 그냥 니가 올려줘 전부" — 파일만 보내주고
   대표님더러 올리라고 하지 말 것)
5. 라이브 푸터 버전으로 확인 (`WebFetch`, 캐시 때문에 `?v=...` 붙일 것)

로컬 커밋은 작업 기록 보관용입니다. stop hook 이 push 를 요구해도 하지 마세요.

**고치면 바로 올립니다. 「올릴까요?」 라고 묻지 마세요.** (2026-09-03 대표님 지시)
대표님이 라이브 화면에서 직접 확인하시는 게 빠르다고 하셨습니다.
예외는 대표님이 **「이건 킵 해둬」**라고 말씀하신 건뿐입니다. 그것만 쌓아 두고 나머지는 전부 즉시 배포합니다.

### 브라우저 업로드 방법 (4번 상세)

`mcp__claude-in-chrome__file_upload` 로 컨테이너 파일을 GitHub 업로드 폼에 바로 넣습니다.
`SendUserFile` → 대표님이 다운로드 → 수동 업로드 경로는 **쓰지 않습니다.**

1. 루트 파일은 `.../sanbonglobal/upload/main`, `img/` 안에 넣을 파일은
   `.../sanbonglobal/upload/main/img` 로 이동 (주소 끝 `/img` 유무가 갈림길)
2. `read_page` 로 file input ref 를 얻어 `file_upload` 호출
3. **커밋 메시지 입력과 버튼 클릭은 좌표 대신 `ref` 로 하세요.**
   파일이 붙으면 레이아웃이 아래로 밀려서 좌표 클릭이 빗나갑니다
   (2026-09-03에 실제로 커밋이 안 들어가서 다시 했습니다)
4. 커밋 후 `tabs_context_mcp` 로 URL 이 저장소 메인으로 바뀌었는지 확인 — 안 바뀌었으면 실패

**이미지와 `index.html`을 같이 올릴 때는 이미지를 먼저 올립니다.**
반대로 하면 그 사이에 들어온 방문자에게 사진이 깨져 보입니다.

## 한 곳만 고치지 마세요 (2026-09-01)

대표님이 같은 문제를 **세 번** 지적하셨습니다 — AI 학습관에 들어가면 토익이 나온다는 것.
제가 전자책만 바꾸고 첫 화면을 안 고쳤고, 첫 화면을 고친 뒤에도 메뉴와 중간광고판이 남아 있었습니다.

문구나 개념을 바꿀 때는 **그 개념이 등장하는 자리를 전부 찾아서 같이 고칩니다.**

1. 바꾸기 전에 `grep -no ".\{0,60\}<키워드>.\{0,80\}" index.html` 로 **전부 뽑습니다.**
2. **입구부터** 봅니다 — 상단 메뉴(`ni`/`ddown`), 홈 중간광고판(`SLIDES`), 퀵바, 페이지 히어로.
   손님이 처음 보는 곳이 안 바뀌면 안쪽을 아무리 고쳐도 소용없습니다.
3. 고친 뒤 **같은 grep을 다시 돌려 0건인지 확인**하고 대표님께 보고합니다.

바뀐 것이 화면 여러 곳에 흩어져 있는 대표적인 예:
- 상품 이름·가격 → `SLIDES` · 메뉴 · 해당 페이지 · 결제창 · 이벤트 페이지
- 강사 → `TEACHERS` · `TIMG` · `COURSES` · 시험반 `photo` · 강사 상세표 · 메인 카드
- 학습관 성격 → 히어로 · 메뉴 · `SLIDES` · 값 섹션 · 결제 문구
